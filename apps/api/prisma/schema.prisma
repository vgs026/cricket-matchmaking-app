generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// Domain models are introduced with their owning feature modules in later phases.
// PostgreSQL is provisioned with PostGIS through infrastructure/docker-compose.yml.
